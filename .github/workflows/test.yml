name: Simple go test workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, edited]
  workflow_dispatch:

jobs:
  tests:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [ '1.25' ]

    steps:
      - uses: actions/checkout@v5
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - name: Install dependencies
        run: go mod download
      - name: Check if builds successfully
        run: go build -o loglint ./cmd/loglint
      - name: Linter checks
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.6
      - name: Test rules with Go
        run: go test -json ./internal/rules > TestRules-${{ matrix.go-version }}.json
      - name: Test analyzer with Go
        run: go test -json ./pkg/analyzer > TestAnalyzer-${{ matrix.go-version }}.json
      - name: Upload Go test results
        uses: actions/upload-artifact@v4
        with:
          name: Go-results-${{ matrix.go-version }}
          path: TestResults-${{ matrix.go-version }}.json
